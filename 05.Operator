연산자

1.산술 연산자
2.대입 연산자
3.논리 연산자
4.비교 연산자

산술 연산자 + , - , * , /, %  등이 있음 

+는 덧셈
-는 뺄셈
*는 곱셈
/는 나눗셈
%는 나눗셈의 나머지

선언 방법
let 입력 = 숫자 + 숫자;
let 입력 = 숫자 - 숫자;
let 입력 = 숫자 * 숫자;
let 입력 = 숫자 / 숫자;
let 입력 = 숫사 % 숫자;

예제
let A = 1 + 2;
let B = 1 - 2;
let C = 1 * 2;
let D = 1 / 2;

==============================================

대입 연산자는 특정 값에 연산을 한 값을 바로 설정 할 때 사용 할 수 있는 연산자

선언 방법
선언한 변수나 상수 += 숫자;
선언한 변수나 상수 -= 숫자;
선언한 변수나 상수 *= 숫자;

