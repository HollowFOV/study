변수와 상수 
1. let 
2. const
3. var 

let는 변수이다 그러므로 바뀔수 있는 수이다.
선언 방법 
let 입력;
let 입력 = 숫자; 
let 입력 = '문자';

예제 
let A; 
let B = 1;
let C = '안녕';

==============================================

const는 상수이다 그러므로 바뀔수 없다.
선언방법
const 입력;
const 입력 = 숫자;
const 입력 = '문자';

예제
cosnt A;
cosnt B = 1;
const C = '안녕';

==============================================

var은 let과 달리 똑같은 이름으로 여러번 선언 가능 하지만 사용하지 말것
선언방법
var 입력;
var 입력 = 숫자;
var 입력 = '문자';

예제
var A;
var A = 1;
var A = '안녕';

